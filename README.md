# Twilio Project (Divineforge)

this project use `yarn` platform in nestJS framework.

## Reference

- Yarn cheatsheet <https://devhints.io/yarn>
- Deploying to heroku <https://www.joshmorony.com/deploying-a-production-nestjs-server-on-heroku/>
- this guide maybe a bit outdated we have to change the package.json `postinstall` to run the `build` instead